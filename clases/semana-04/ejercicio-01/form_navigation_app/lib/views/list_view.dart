import 'package:flutter/material.dart';

class ListProductsView extends StatelessWidget {
  const ListProductsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('List Products View'),
      ),
    );
  }
} 